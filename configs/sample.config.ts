import type { Config } from "../src/features/config/schema";

const sampleConfig: Config = {
  name: "Sample Config",
  primaryColor: "#294C60",
  primaryLogo: "/assets/sample/logo.svg",
};

export default sampleConfig;
